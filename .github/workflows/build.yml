name: build
on: [push]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make release
      - uses: actions/upload-artifact@v4
        with:
          path: base.rar
          if-no-files-found: error
          overwrite: true
